# Conjuntos

# Union de Conjuntos | Cruce | Resta de Conjuntos | Diferencia Simetrica
# set() para convertir una lista a conjunto
# .issubset() para ver si es el subconjunto, .issuperset() para ver si es el superconjunto, .isdisjoint() para ver si estan totalmente descontectados uno del otro
# frozenset() funciona para bloquear los cambios en un conjunto

"""
print(a==b)
c =  a | b
c = a & b
c = a - b
c = b - a
c = a ^ b

a = {1,2,3}
b = {4,5,2}
c = frozenset({1,2,3,4,5})
print(a.issubset(c))
print(c.issuperset(a))
print(a.isdisjoint(b))
"""